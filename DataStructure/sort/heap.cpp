#define COMPARE <
#include<iterator>
using std::end;
using std::swap;

template<typename T>
void heap_sort(T list[])
{
	
}

template<typename T>
void build_heap(T list[])
{

}

template<typename T>
void heapify(T list[], unsigned index)
{
	unsigned size = end(list) - std::begin(list);
	while (index < size / 2)
	{

	}
}